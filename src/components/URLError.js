//URL Error component. This js file displays an error message when called.
import React from 'react';

const URLError = () =>(
  <li>
    <h3>404 Error has occured</h3>
    <p>The url does not exist</p>
  </li>
);

export default URLError;
